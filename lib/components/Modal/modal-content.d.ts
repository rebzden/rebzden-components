import { ModalState } from ".";
interface Props {
    change: ModalState;
}
export declare const ModalContent: import("styled-components").StyledComponent<"div", import("../../presets/theme").Theme, Props, never>;
export {};
