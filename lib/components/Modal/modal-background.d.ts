import { ModalState } from ".";
interface ModalBackgroundProps {
    open: boolean;
    change: ModalState;
}
export declare const ModalBackground: import("styled-components").StyledComponent<"div", import("../../presets/theme").Theme, ModalBackgroundProps, never>;
export {};
