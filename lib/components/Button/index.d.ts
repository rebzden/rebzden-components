import { ColorType } from '../../presets/theme';
export interface IButton {
    type?: ColorType;
}
export declare const Button: import("styled-components").StyledComponent<"button", import("../../presets/theme").Theme, IButton, never>;
