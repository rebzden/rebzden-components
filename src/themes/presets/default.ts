import { ITheme } from "../theme";

const theme = {
    main: 'red',
    padding: {
        none: '0px',
        small: '5px',
        medium: '10px',
        big: '15px',
    }
} as ITheme;

export default theme;